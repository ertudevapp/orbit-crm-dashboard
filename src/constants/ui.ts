/**
 * Common constants used across the application
 */

/**
 * Box shadow for subtle elevation
 * Used for cards, buttons, and other elevated elements
 */
export const BOX_SHADOW_SM = '0 1px 2px 0 rgb(0 0 0 / 0.05)';

/**
 * Box shadow for medium elevation
 */
export const BOX_SHADOW_MD = '0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)';

/**
 * Box shadow for high elevation
 */
export const BOX_SHADOW_LG = '0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)';
